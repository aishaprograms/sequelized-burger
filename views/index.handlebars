<div class="row">
	<div class="col-md-6">
		<h2>Burgerz, Bon Appétit!</h2>
		<hr>
		<ul>
		  {{#each burgers}}
		    {{#unless this.devoured}}
		      <li>
		        {{this.burger_name}}
		        <form class="form-inline" action="/{{this.id}}?_method=PUT" method="POST">
		          <input type="hidden" name="devoured" value="false">
		          <button class="btn btn-success" type="submit">Devour it!</button>
		        </form>
		      </li>
		    {{/unless}}
		  {{/each}}
		</ul>
	</div>
	<div class="col-md-6">
		<h2>Burgerz Consumed</h2>
		<hr>
		<ul>
			  {{#each burgers}}
			    {{#if this.devoured}}
			      <li>
			        {{this.burger_name}}
			      </li>
			    {{/if}}
			  {{/each}}
		</ul>
	</div>
</div>
<hr>
<hr>
<div class="row text-center">
	<h2><span class="red">Add a Burger</span></h2>
	<form action="/" method="POST">
	  <div class="form-group">
	    <label for="burger_name">Burger Name:</label>
	    <input type="text" id="burger_name" name="burger_name">
	  </div>
	  <button class="btn btn-success" type="submit">Submit</button>
	</form>
</div>
<br>
<br>
